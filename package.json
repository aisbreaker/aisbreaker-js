{
  "name": "aisbreaker-js",
  "version": "0.0.19",
  "description": "This is the git monorepo for all AIsBreaker core packages/modules. It's only to combine all npm packages/modules, it's not published as separate npm package/module",
  "type": "module",
  "author": "aisbreaker",
  "license": "MIT",
  "workspaces": [
    "packages/*" 
  ],
  "devDependencies": {
  },
  "dependencies": {
  }
}
